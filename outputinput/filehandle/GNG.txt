grabandgo
char ch;
	while(!file.eof()){
		file.get(ch);
		cout<<ch;
	}
	file.close();
grabandgo
char ch;
	while(!file.eof()){
		file.get(ch);
		cout<<ch;
	}
	file.close();

grabandgo
char ch;
	while(!file.eof()){
		file.get(ch);
		cout<<ch;
	}
	file.close();

grabandgo
char ch;
	while(!file.eof()){
		file.get(ch);
		cout<<ch;
	}
	file.close();